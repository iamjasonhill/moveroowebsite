/**
 * Explicit public modification timestamps for routes whose XML sitemap entries
 * need a truthful `<lastmod>` value. Do not derive these from build or deploy time.
 */
/** @type {Record<string, string>} */
export const routeLastModified = {
	"/household-vehicle-relocation/": "2026-07-21T00:00:00+10:00",
};
